import React, { useEffect, useRef } from 'react'

const Useref = () => {

    const ref = useRef()

    // useEffect(()=>{
    //     ref.current.focus()
    // })
  return (
    <>
        {/* <input type="text" ref={ref}/> */}
    </>
  )
}

export default Useref
